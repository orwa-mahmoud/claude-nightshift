{
  "preset": {
    "name": "balanced-docs",
    "branchMode": "feature-branch",
    "allowedSources": "closed-list",
    "verificationProfile": "balanced",
    "resourceLimits": {"maxRps": 1000}
  },
  "rules": {
    "retention": {"archiveDays": 90},
    "branchMode": "default"
  },
  "shiftDefaults": {
    "verificationProfile": "balanced",
    "toolingPolicy": "existing-tools"
  }
}
