{
  "user": "checkout operator",
  "problem": "failed payments require manual retry",
  "evidence": ["support tickets mention retry", "src/webhooks/payment.ts lacks retry"],
  "expectedOutcome": "automatic retry with backoff reduces manual intervention",
  "reversibility": 4,
  "measurement": "fewer support tickets mentioning payment retry",
  "sliceMinutes": 45,
  "rollbackBoundary": "feature flag payment.retry.enabled defaults off",
  "priorReceipts": [
    {
      "area": "full payment rewrite",
      "outcome": "rejected",
      "reason": "too large for one shift"
    },
    {
      "area": "synchronous blocking retry in request path",
      "outcome": "disproved",
      "reason": "prior receipt showed timeout regressions"
    }
  ],
  "alternatives": [
    {
      "name": "queue-based retry",
      "rejectedReason": "requires new infra the owner has not authorized"
    }
  ]
}
