{
  "objective": "Add retry logic to the payment webhook handler with tests",
  "hours": 3,
  "workTarget": "/repo",
  "evidence": ["src/webhooks/payment.ts", "tests/webhooks/payment.test.ts"],
  "underspecified": ["max retries", "backoff strategy"],
  "defaults": {
    "max retries": "3 attempts with exponential backoff starting at 250ms",
    "backoff strategy": "exponential with jitter, capped at 30s"
  }
}
