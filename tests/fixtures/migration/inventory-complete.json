{
  "migrationName": "users-table-v2",
  "authoritativeGuidance": "docs/migrations/users-v2.md",
  "consumers": [
    {"name": "api-server", "kind": "service"},
    {"name": "batch-export", "kind": "job"}
  ],
  "persistedState": [{"store": "postgres", "schema": "public.users", "version": 1}],
  "ordering": ["expand-schema", "backfill", "dual-write", "cutover", "contract"],
  "defaultsNullability": [{"field": "display_name", "default": "''", "nullable": true}],
  "backfillRequired": true,
  "locks": [{"name": "users_migration", "active": false}],
  "oldNewOverlap": {"dualWrite": true, "readFallback": "v1", "duration": "7d"},
  "idempotent": true,
  "rollbackSteps": ["disable-dual-write", "restore-v1-read", "drop-v2-columns"],
  "stagedChanges": ["add nullable columns", "add compatibility view"],
  "representativeData": {"present": true, "sampleSize": 100, "anonymized": true}
}
