# Parking Lot

- **Retire `/old-pricing` with or without a redirect?** The broken link into it is fixed (item 1),
  but the URL itself may still be bookmarked or indexed. Default: no redirect added tonight — the
  fix only repoints the one link found. Chosen because adding a redirect touches the site's
  hosting config, which stays an owner decision.
