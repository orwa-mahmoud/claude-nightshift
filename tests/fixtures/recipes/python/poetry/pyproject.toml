[tool.poetry]
name = "nightshift-fixture"
version = "0.1.0"
description = "Fixture project for the python recipe tests."
authors = []
packages = [{ include = "nightshift_fixture", from = "src" }]

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
