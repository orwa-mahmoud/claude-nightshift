{
  "capabilityId": "test",
  "ecosystems": ["make"],
  "versionConstraints": {"make": ">=3.81"},
  "detect": {
    "manager": "make",
    "signals": ["Makefile"],
    "targets": ["test"],
    "command": "grep -Eq '^test[[:space:]]*:' Makefile"
  },
  "probe": {
    "command": "make -n test",
    "provisionedWhen": "exit-zero"
  },
  "packageManagerAdditions": [],
  "allowedFiles": [],
  "minimalConfig": {},
  "smoke": {
    "command": "make --version >/dev/null 2>&1 || exit 127; make -n test"
  },
  "rollback": {"command": "true", "removes": []},
  "enabledShifts": [
    "quality",
    "clear-quality-debt",
    "coverage-hunt",
    "defect-hunt",
    "flaky-test-repair"
  ],
  "safetyClass": "local-dev-free",
  "permissionRequirements": [
    "fallback: the Makefile target is the tool — nothing is installed, and a project without a test target has no test capability",
    "fallback: the smoke is a dry run, so provisioning never runs the suite the shift will run",
    "fallback: a dry run still expands the Makefile, so a variable defined through a shell call is evaluated when the target is read"
  ],
  "elevationCategories": [],
  "recipeVersion": "1",
  "lastVerified": "2026-09-02",
  "upstream": "https://www.gnu.org/software/make/",
  "maintenance": {
    "check": "the recipe reads targets and dry-runs them; it breaks only if make drops -n or --version, so re-verify against the current make manual",
    "resolves": "make --version"
  }
}
