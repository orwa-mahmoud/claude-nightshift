{
  "capabilityId": "documentation-link",
  "ecosystems": ["make"],
  "versionConstraints": {"make": ">=3.81"},
  "detect": {
    "manager": "make",
    "signals": ["Makefile"],
    "targets": ["docs"],
    "command": "grep -Eq '^docs[[:space:]]*:' Makefile"
  },
  "probe": {
    "command": "make -n docs",
    "provisionedWhen": "exit-zero"
  },
  "packageManagerAdditions": [],
  "allowedFiles": [],
  "minimalConfig": {},
  "smoke": {
    "command": "make --version >/dev/null 2>&1 || exit 127; make -n docs"
  },
  "rollback": {"command": "true", "removes": []},
  "enabledShifts": ["documentation-writing", "documentation-drift"],
  "safetyClass": "local-dev-free",
  "permissionRequirements": [
    "fallback: the Makefile target is the tool — nothing is installed, and a project without a docs target has no documentation capability",
    "fallback: the target is whatever the project already builds or checks under docs; the recipe never publishes and never fetches a URL",
    "fallback: a dry run still expands the Makefile, so a variable defined through a shell call is evaluated when the target is read"
  ],
  "elevationCategories": [],
  "recipeVersion": "1",
  "lastVerified": "2026-09-02",
  "upstream": "https://www.gnu.org/software/make/",
  "maintenance": {
    "check": "the recipe reads targets and dry-runs them; it breaks only if make drops -n or --version, so re-verify against the current make manual",
    "resolves": "make --version"
  }
}
